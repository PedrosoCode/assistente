import 'package:flutter/material.dart';

class ComecarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Começar'),
      ),
      body: Center(
        child: Text('Tela de Começar'),
      ),
    );
  }
}
